import { LoggerColorPipe } from './logger-color.pipe';

describe('LoggerColorPipe', () => {
  it('create an instance', () => {
    const pipe = new LoggerColorPipe();
    expect(pipe).toBeTruthy();
  });
});
